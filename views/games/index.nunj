{% extends 'base/index.nunj' %}


{% block content %}
    <div class="column">
        GAMES:
        {% for game in games %}
            <div class="box">{{ game.datetime.toLocaleDateString() }} {{ game.place }}</div>
        {% endfor %}
    </div>
{% endblock %}